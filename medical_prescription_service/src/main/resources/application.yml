spring:
  application:
    name: medical_prescription_service

  datasource:
    url: "jdbc:mysql://localhost:3312/medical_prescription_service"
    username: root
    password: root

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update

server:
  port: 8088
  servlet:
    context-path: /api/v1

